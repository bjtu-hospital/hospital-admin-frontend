
提示词0:
```
下面请完成医院科室的管理模块.
首先医院分为n个大的科室,大的科室下面有详细的科室
接口在对应的文件中.
请你以合适的形式展示详细科室.(上面有个导航栏.是大的科室名称,点击可以查看其子科室, 并且还有一个查看全部,和搜索科室的功能. 点击科室可以查看科室的详细信息. 并且可以在这个页面可以修改科室的信息,或者删除科室(需要确认).(这里用到的函数,请你先留空,后续我会补充对应的逻辑))
并且需要提供一个新增科室类别,可以新增大类. 还有一个新增具体科室的按钮. 点击, 可以选择这个科室是属于哪个大科室. 名称,以及描述.
请你美观,合理的进行布局.
```






提示词1:
```
请你完成下面的任务;
1.修复问题:当页面的数据加载失败的时候, // 计算当前页显示的科室
const paginatedDepartments = computed(() => {
  const start = (currentPage.value - 1) * pageSize.value
  const end = start + pageSize.value
  return filteredMinorDepartments.value.slice(start, end)
})
这个函数会报错! 请你修复相关的逻辑,使其只有在页面有数据的时候才正常的计算.

2. 当前页面的布局分了四层,可是管理的搜索栏(新增大科室, 新增小科室), 查看全部(大科室列表), 详细科室, 页面.
第一栏太大了.把科室管理四个字和对应的小标题去掉,将新增大科室,详细科室.和搜索栏放在一行.然后大科室一栏最左侧给一个编辑的按钮. 点击编辑按钮,可以选择编辑大科室的信息. (名称,描述,删除).
当一删除一个大科室的时候, 如果其还有子科室(详细科室),那么会拒绝删除 (前端也请做一下简单的判断)
{
  "code": 99,
  "message": {
    "error": "认证时出现异常",
    "msg": "内部服务异常"
  }
}

```


提示词2:
```
请你完成下面的任务;
1. 将搜索栏占放在左侧,并且占满,两个按钮剩余的空间.
2. 编辑页面统一采用,大科室编辑的那个降低背景为暗色的效果.
3. 在页面的最下面,可以选择在这个页面,展示的详细科室的数量9,12,15,17,21,all.

```